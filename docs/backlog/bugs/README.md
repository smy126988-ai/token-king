# Bug 库

> 流转: 发现 → 已诊断待修 → 修复中 → 验收中 → ✅已修
> 会话级 TaskCreate 是临时的，这里是持久台账。

| ID | 现象 | 状态 | 根因(已诊断) | 修法 |
|----|------|------|-------------|------|
| B01 | 金额/货币显示变美元 | ✅已修 | `ProviderRegionTests` 把 `currency.selected` 写成 USD 的测试污染 | commit `7d1363c` 恢复 currency；但仍需根治测试初始化 |
| B02 | Provider wiring 遗漏 | ✅已修（commit `403f72a`） | `ProviderManager` 漏注册 `OpenCodeProvider`；`CLIProviderManager` 漏 `.openCode`/`.tavilySearch`/`.braveSearch`；`ProviderMenuBuilder` 对多个 provider 走 default | commit `403f72a`：`ProviderManager.makeDefaultProviders()` 新增 `OpenCodeProvider()`（原来只有 Zen/Go）；`CLIProviderManager.registeredProviders` 与 `init` 的 `providers` 数组同时补齐 `.openCode`/`OpenCodeProvider()` 与 `.tavilySearch`/`.braveSearch`。`ProviderMenuBuilder` 的 `openCode`/`.tavilySearch`/`.braveSearch` 子菜单与 `configInfo` 的 switch case 修复跟进见 B17/B20/B24。Regression 测试：`ProviderUsageTests.testOtherProvidersRemainEnabledByDefault`（断言 `.openCode.isEnabled == true`，line 430） |
| B03 | `usagePercentCandidates` 重复 case | 已诊断待修 | `StatusBarController.usagePercentCandidates` 中 `.volcanoArk` 与 `.mimo`/`.hunyuan`/`.zhipuGLM` 有重复分支 | 去重/合并 |
| B04 | 图标映射错误/缺失 | 已诊断待修 | `ModernStatusBarIconView` 和 `MultiProviderStatusBarIconView` 里 `.zhipuGLM` 仍用 `g.circle`；`.minimaxCodingPlanCN` 在 Modern 未处理 | 换 `z.circle`；补 MiniMax CN 图标 |
| B05 | 国服 provider region 默认值错误 | 已诊断待修 | `.mimo`、`.volcanoArk`、`.hunyuan`、`.zhipuGLM` 是国服 provider，但 `region` 默认 `.global` | 默认值改为 `.china` |
| B06 | MiniMax 旧档位不兼容 | 已诊断待修 | 档位从 Starter/Plus HS/Max HS/Ultra HS 改成 Plus/Max/Ultra 后，老用户存储的旧档位菜单里不可再选 | 迁移旧档位名或做 fallback |
| B07 | 测试污染/Flaky tests | 已诊断待修 | `ProviderResultTests` 用 `UserDefaults.standard` 且只通过 `addTeardownBlock` 恢复，有次 flaky；多个测试在 `StatusBarController()` init 时触发 UserDefaults 写入 | 用独立 UserDefaults suite 或严格 teardown |
| B08 | StatusBarControllerTests 未清理共享状态 | 已诊断待修 | 测试直接 `UserDefaults.standard.set(true, forKey: "githubStarPromptDismissed")` 且从不恢复；每个用例都新建 `StatusBarController()`，其 init 会向 `UserDefaults.standard` 写入 `braveRefreshMode` 默认值、启动 timer/background task | 使用独立 UserDefaults suite 或在 setUp/tearDown 中保存/恢复相关 key；测试用 StatusBarController 应提供可注入 defaults 的初始化或禁用后台任务 |
| B09 | StatusBarController init 硬编码 UserDefaults.standard 且启动后台任务 | ✅已修（基础能力） | `StatusBarController.init()` 直接读写 `UserDefaults.standard`（refreshInterval/predictionPeriod/displayMode/braveRefreshMode/githubStarPrompt 等），并调用 `CurrencyFormatter.shared.refreshRateInBackground()` 和 `startRefreshTimer()` | 加 `InitOptions` 结构体（`production` / `testing(userDefaults:)`），新签名 `init(options: InitOptions = .production)`，AppDelegate 改用 `.production`。两个 computed property `userDefaults` / `currencyFormatter` 把全部 71 个 `UserDefaults.standard` / `CurrencyFormatter.shared` 入口路由到注入实例；后台 Timer / GitHub star prompt / debug 日志走 `runBackgroundTasks` / `promptGitHubStar` / `logDebugEnvironmentInfo` 三个 flag。新增 `StatusBarControllerTestingModeTests`（3 个断言测试 init 模式不写 `.standard`、写入 suite、production 行为保持不变），并且 `StatusBarControllerTests` 的 4 个旧 case 也迁到 `.testing(userDefaults:)` 路径。**注意：B09 不等于 B07/B08/B10/B11/B13/B14/B15 全部修完**——那些测试污染 bug 的根因是 `StatusBarController.init()` 硬编码 `.standard`，这条已经切到 facade；具体每个测试是否真不再污染需要单独审一遍 |
| B10 | ProviderResultTests 使用 UserDefaults.standard 且依赖 addTeardownBlock | 已诊断待修 | 测试直接读写 `UserDefaults.standard` 的 currency/rate key，通过 `addTeardownBlock` 恢复；崩溃或提前退出时恢复不执行；同时污染共享 `CurrencyFormatter`/`ExchangeRateStore` | 改用独立 `UserDefaults(suiteName:)` 注入 `CurrencyFormatter` 和 `ExchangeRateStore` |
| B11 | ProviderRegionTests 修改 CurrencyFormatter.shared 并创建 StatusBarController | 已诊断待修 | 测试用 `defer` 恢复 `CurrencyFormatter.shared.currency`，但创建 `StatusBarController()` 会触发 UserDefaults 写入和后台任务；`defer` 在崩溃时不保证执行 | 注入独立 formatter/defaults；避免在区域测试中初始化完整 status bar controller |
| B12 | SubscriptionSettingsManager 单例写 UserDefaults.standard | 已诊断待修 | `SubscriptionSettingsManager.shared` 直接读写 `UserDefaults.standard`；`ProviderRegionTests` 和 `ProviderUsageTests` 向其写入 subscription_v2.* 数据，仅对特定 key defer 清理 | 给 SubscriptionSettingsManager 增加 `UserDefaults` 注入初始化；测试使用独立 suite |
| B13 | BraveSearchProviderTests 未保留原始 UserDefaults | 已诊断待修 | setUp/tearDown 只 `removeObject` Brave 相关 key，不保存/恢复用户原有值；多个测试并行时互相覆盖 | setUp 保存原值，tearDown 恢复；或改用注入的 UserDefaults |
| B14 | ProviderUsageTests 读取 ProviderManager.shared 与 SubscriptionSettingsManager.shared | 已诊断待修 | `testProviderManagerDefaultProvidersExcludeKiro` 使用 `ProviderManager.shared` 单例；subscription 测试使用 `SubscriptionSettingsManager.shared`，未重置缓存 | 使用自定义 `ProviderManager(providers:)` 注入；SubscriptionSettingsManager 注入独立 defaults |
| B15 | 多个 Provider 测试使用 .shared tokenManager/session | 已诊断待修 | `VolcanoArkProviderTests`、`MiniMaxProviderTests`、`KimiProviderTests` 部分用例传入 `tokenManager: .shared` / `session: .shared`，依赖全局状态和真实网络 | 全部使用 mock tokenManager 和 `MockURLProtocol` 注入 session |
| B16 | CLIProviderManager 注册列表与实例数组不一致 | ✅已修（commit `403f72a`） | `CLIProviderManager.registeredProviders` 包含 `.tavilySearch`/`.braveSearch`，但 `init` 的 `providers` 数组未实例化对应 Provider；`.openCode` 既未出现在 `registeredProviders` 也未实例化 | commit `403f72a`：`CLI/CLIProviderManager.swift` 同时修复 `registeredProviders` 与 `init()` 的 providers 数组——补 `OpenCodeProvider()`、`TavilySearchProvider()`、`BraveSearchProvider()`；两端 `let xxxProvider = ...Provider()`、进入 `self.providers` 数组的顺序完全对齐 registeredProviders 顺序（CLAUDE/Copilot 均上线）。Regression 覆盖由 B02 + B23 测试组合保证（`ProviderUsageTests.testOtherProvidersRemainEnabledByDefault` 与 `testProviderManagerDefaultProvidersExcludeKiro`） |
| B17 | ProviderMenuBuilder 对 openCode/tavilySearch/braveSearch 走 default | 已诊断待修 | `createDetailSubmenu` 的 switch 缺少 `.openCode`、`.tavilySearch`、`.braveSearch` 分支，导致 detail 子菜单只有通用 trailing 行 | 补这三个 provider 的专属 case：openCode 显示额度/用量；搜索 provider 复用 `createSearchEngineRows` 或显示用量/错误 |
| B18 | ModernStatusBarIconView 图标映射与 ProviderProtocol 不一致 | 已诊断待修 | `.zhipuGLM` 仍用 `g.circle`（ProviderProtocol 已改为 `z.circle`）；`.minimaxCodingPlanCN` 未处理；`.geminiCLI` 使用 `sparkles` 与 ProviderProtocol 的 `g.circle` 不一致 | 统一优先读取 `identifier.iconName` 或显式同步所有 case；补 `.minimaxCodingPlanCN`；zhipuGLM 改 `z.circle` |
| B19 | ProviderMenuBuilder 中 synthetic/kiro 出现重复分隔线 | 已诊断待修 | `.synthetic`、`.kiro` 的 case 在调用 `addSubscriptionItems` 前手动 add separator，而 `addSubscriptionItems` 内部又 add separator | 移除 `.synthetic`、`.kiro` case 中多余的 `submenu.addItem(NSMenuItem.separator())` |
| B20 | StatusBarController.configInfo 多个 provider 走 default | 已诊断待修 | `configInfo(for:)` switch 对 `.tavilySearch`、`.braveSearch`、`.geminiCLI`、`.cursor`、`.commandCode`、`.kiro` 等使用 default 提示 "对应 provider 的 key 字段" | 补全缺失 provider 的配置字段名与路径，保持与 TokenManager key 读取逻辑一致 |
| B21 | 启用 Gemini CLI 多账户时 quota 菜单出现重复条目 | 已诊断待修 | `.geminiCLI` 同时出现在 `providerQuotaOrder` 与 `updateMultiProviderMenu` 后的专门处理块中；循环按 aggregate usage 插入一行，后续块又为每个 `geminiAccount` 插入一行 | 从 `providerQuotaOrder` 移除 `.geminiCLI`，或在循环中跳过 Gemini（保留专门的 per-account 块） |
| B22 | 同名订阅套餐被同时高亮 | 已诊断待修 | `ProviderMenuBuilder.addSubscriptionItems` 通过 `selectedName == preset.name` 判断选中，未比较 cost；Gemini CLI 有两个 Plus（$4/$8）、两个 Ultra（$125/$250） | 选中判断同时比较 name 与 cost，或改用 `item.representedObject` 中的 plan 匹配 |
| B23 | CLI 与 App 对 `.kiro` 启用策略不一致 | ✅已修（commit `403f72a`） | `ProviderIdentifier.kiro.isEnabled == false`，`ProviderManager` 过滤掉；`CLIProviderManager.registeredProviders` 与 providers 数组仍包含 `.kiro` | commit `403f72a`：采用「统一移除」路线——`CLIProviderManager.registeredProviders` 删除 `.kiro`；`init()` 同步移除 `KiroProvider()`（不再实例化、不再放进 providers 数组）。CLI 与 App 现在都遵循 `isEnabled` 旗标，意图完全一致。Regression 测试：`ProviderUsageTests.testKiroIsDisabledByDefault`（line 424：断言 `.kiro.isEnabled == false`）+ `testProviderManagerDefaultProvidersExcludeKiro`（line 436：断言 `ProviderManager.shared.getAllProviders()` 不含 `.kiro`）|
| B24 | 搜索引擎「点击配置」提示信息无意义 | 已诊断待修 | `StatusBarController.configInfo(for:)` 对 `.tavilySearch`/`.braveSearch` 走 default，返回「对应 provider 的 key 字段」与通用路径 | 补 `.tavilySearch`、`.braveSearch` 的 fieldName 与配置路径 |
| B25 | 按量付费区写死仅 .openRouter/.openCodeZen | 已诊断待修 | `updateMultiProviderMenu` 与分享快照均硬编码 pay-as-you-go provider 顺序；即使 `.openCode` 等被启用也不会出现 | 按 `ProviderType.payAsYouGo` 与启用状态动态收集 providers |
| B26 | 预测周期菜单状态与实际显示不同步 | 已诊断待修 | `setupMenu` 创建并保存 `predictionPeriodMenu`，但该菜单从未挂到可见菜单；`updatePredictionPeriodMenu()` 只更新这个隐藏菜单 | 移除属性，或让可见的预测周期子菜单复用/同步该菜单状态 |
| B27 | 订阅菜单「该版本仅海外」提示不可达 | 已诊断待修 | `addSubscriptionItems` 中 `visiblePresets.isEmpty` 仅在 `hasCNYPresets && 无 cnyCost` 时成立，而 `hasCNYPresets` 定义即为存在 cnyCost，逻辑互斥 | 删除该死代码分支 |
| B28 | 用量历史菜单项已创建但从未加入主菜单 | 已诊断待修 | `setupMenu` 中初始化 `historyMenuItem`/`historySubmenu` 后，`menu.addItem(historyMenuItem)` 被注释掉 | 移除死属性，或确认是否需要恢复历史入口 |
| B29 | 动态菜单项使用 magic number `999` 作为 tag | 已诊断待修 | `updateMultiProviderMenu` 用 `tag == 999` 识别并清理动态插入的项，无命名常量 | 定义 `private enum MenuItemTag { static let dynamic = 999 }` 等命名常量 |
| B30 | Gemini CLI 订阅档位出现重复名称 | 已诊断待修 | `ProviderSubscriptionPresets.geminiCLI` 里 "Plus"/"Ultra" 各出现两次不同价格 | 重命名档位或删除重复项，使菜单名称唯一 |
| B31 | ProviderRegionTests 未覆盖新国服 provider 的 region 默认值 | 已诊断待修 | 测试只断言 `.kimiCN`/`.minimaxCodingPlanCN` 为 `.china`，未覆盖 `.mimo`/`.volcanoArk`/`.hunyuan`/`.zhipuGLM` | 补充 region 断言（应为 `.china`），作为 B05 回归测试 |
| B32 | 国内 provider USD 订阅价使用固定汇率 7.2 | 已诊断待修 | `volcanoArk`/`hunyuan`/`zhipuGLM` 的 cost 用 `1.0/7.2` 硬编码，而 `CurrencyFormatter` 使用实时汇率 | USD 模式下用 `cnyCost / formatter.currentRate` 计算，或以 CNY 为唯一真值统一换算 |
| B33 | 死代码状态栏视图硬编码 "$" | 已诊断待修 | `ModernStatusBarIconView` / `MultiProviderStatusBarIconView` 多 provider 分支写死 `$`，与货币设置不一致 | 删除死代码或接入 `CurrencyFormatter` |
| B34 | MiMo 订阅预设注释与国服定位矛盾 | 已诊断待修 | 注释写 "仅海外 Token Plan"，但 MiMo 被归为国服且预设含 `cnyCost` | 修正注释，必要时按 region 拆分 preset catalog |
| B35 | Kimi CN provider 没有 fallback 到 legacy global key | ✅已修 | `getKimiCNAPIKey()` 只读 `kimi-for-coding-cn` 字段，老用户把国内 key 写在 `kimi-for-coding`（global 字段）下时 KimiCNProvider 失败，KimiGlobalProvider 接管 → 显示海外档 + USD×rate | `getKimiCNAPIKey()` 加 fallback `auth.kimiForCodingCN?.key ?? auth.kimiForCoding?.key`，镜像 MiniMax 既有 fallback 模式；新增 2 个 TokenManagerTests 回归 |
| B36 | MiniMax 重置时间显示固定窗口而非 Dashboard 滑动窗口 | ✅已修 | `resetDateFromMiniMaxFields` 偏好绝对 `end_time`（固定窗口），与 Dashboard 实际显示的滑动窗口不一致 | 改回偏好 `remains_time`（滑动：now + remainsTime）；`end_time` 降级为 fallback；更新 2 个 MiniMax 测试断言 |
| B37 | Kimi CN fallback 后出现重复 Kimi 入口 | ✅已修 | 加 `getKimiCNAPIKey` fallback 后，老用户只有 global key 时 KimiCN 和 KimiGlobal 同时激活，菜单出现两条 Kimi | `getKimiAPIKey` 在 CN key 缺失但 global key 存在时返回 nil（让 CN 通过 fallback 独占 key）；新增 2 个 TokenManagerTests 回归 |
| B38 | 「无」与 API 检测到的套餐同时 .on | ✅已修 | `addSubscriptionItems` 中 `noneItem.state` 和 preset loop 的 `case .none where detectedPlanName == preset.name` 两个判断独立，都会在 `currentPlan==.none && detectedPlanName 命中 preset` 时成立 | 「无」改为仅在 `currentPlan==.none && !hasDetectedMatch` 时 .on；preset loop 行为保留；新增 3 个 SubscriptionPlanDisplayTests 回归 |
| B39 | 多显示器下 replicant 的 NSMenu 同步有时序漏洞 | 部分缓解（风险未闭环验证） | `AppDelegate.syncMenuToAllStatusWindows` 只在 `attachStatusItem` 和 `applicationDidFinishLaunching` 中各跑一次。SwiftUI 可能在 `syncMenuToAllStatusWindows` 跑完之后才惰性创建第二个 `NSSceneStatusItem`（特别是 `screensHaveSeparateSpaces=YES` 但 secondary display 处于低功耗状态时）。`NSApplication.didChangeScreenParametersNotification` 只覆盖显示器热插拔，不覆盖 SwiftUI 自身 Scene 的延迟创建 | 加 `scheduleResyncAfterLaunch()`（`asyncAfter` 1.0s 后重试一次）+ `reSyncMenu` 节流 60s（避免热插拔刷屏）+ `syncMenuToAllStatusWindows` 改用 `Mirror.descendant` 读 `statusItem`，不再走 `value(forKey:)` 防 NSException。**未确认闭环**：未在多显示器环境实跑重现；菜单刷新后没有兜底 sync 是因为主链复用同一个 NSMenu 引用（reference-shared），但这条还没在实测多屏切换时验证 |
| B40 | macOS 26.x 多显示器下 replicant 的菜单栏图标仍是 SwiftUI 默认 gauge | 已诊断待修 | macOS 26.x 在 `screensHaveSeparateSpaces=YES` 下，secondary `NSSceneStatusItem` 上 `button == nil`（lldb 已验证）。我们 `renderStatusItemImage()` 只写 `button.image`，replicant 这个写入是 no-op；菜单虽已通过 `setMenu:` 同步好了（Bug 已修），但**图标本身** SwiftUI 用的是 `Image(systemName: "gauge.medium")` 作为 label，replicant 上 macOS 不再像 pre-26 `NSStatusItemReplicant` 那样自动镜像 primary 的 `button.image` | 暂未实现。两条思路：(1) fork `MenuBarExtraAccess` 尝试把 secondary instance 上 MenuBarExtra 的 label 直接换成 `gauge.with.dots.needle.bottom.50percent` 之类的稳定 SF Symbol（侵入 label 路径，等同当年撤销的 PR 路线，需要谨慎）；(2) 找出 macOS 在 NSSceneStatusItem 上替代 `button.image` 的官方接口（如果有）。目前优先级低：用户当前只用单屏，B39 修完后多屏才有现实意义。**注：commit `96d5303` 修了 B41（primary 单屏图标模糊），可能顺带也缓解了本条**：B41 修复 = 不再设 `button.image`，让 subview 自己画。secondary 上 subview 不会被添加（因为 `attachStatusIconViewToButton` 没在 replicant item 上跑过），所以 secondary 现在显示的是 SwiftUI `Image(gauge.medium)` label — 跟 B40 描述的现象一致但路径不同。**状态不变**：B40 等用户上 multi-monitor 后重测 |
| B41 | 主菜单栏 gauge.medium 图标看起来软/模糊（与 Terminal/Battery 等图标对比） | ✅已修（commit `96d5303` + `5fe507c` + `5cadb28`） | `renderStatusItemImage()` 在 `StatusBarController.swift:488` 用 22pt × `NSScreen.main.backingScaleFactor` 构造 NSBitmapImageRep 然后 `button.image = image`。lldb + screencap ASCII diff 显示：**真正可见的菜单栏图标是 `attachStatusIconViewToButton()` 加进去的 `StatusBarIconView` subview**，不是 `button.image`，SwiftUI label 也不是。每次 `renderStatusItemImage()` 把 `button.image` 设成 22pt 宽的空 bitmap，结果是 macOS 按 22pt 框绘制 subview，把 SF Symbol 压进约 16pt，刻度和指针被 AA 糊掉。证据有三条：(a) 我的 A_production 镜像（仅镜像 `renderStatusItemImage` 那条）mean alpha 30.7，live 截屏抠出的 icon 是 alpha 255；(b) 把 `Image(systemName:)` 改成 `star.fill` 在 ModernApp.swift 没效果，把 `currentIconSymbolName` 改成 `star.fill` 在 StatusBarIconView.swift 立刻看到星形；(c) 注释掉 `button.image = image` 后，菜单栏图标从 16pt 涨到 17.5pt，外圈、刻度、指针全部清晰不透明 | commit 三连修：**`96d5303`** 删 22pt bitmap + `button.image = image` 那行让 subview 独占 17.5pt 自然尺寸。**`5fe507c`** 跟进：在 `renderStatusItemImage()` 里设 `button.image = nil` 顶替 SwiftUI `MenuBarExtra` 默认写进去的 `NSSymbolImageRep(gauge.medium)`（不然 subview 跟 SwiftUI label 重叠）。**`5cadb28`** 收尾：直接改 `ModernApp.swift` 把 SwiftUI label 从 `Image(systemName: "gauge.medium")` 换成 `Color.clear.frame(width:1, height:1)`，因为 SwiftUI 会周期性重新评估 Scene body，每次都把 label 重新塞进 `button.image`，造成「间歇性双图标」闪烁——`5fe507c` 治不了，得从源头不画 label。351/351 测试通过；live 截屏对比像素差 690px（修复前/后）；~45s 观察期 KVO `button.image` 只 fire `→ nil`，不再出现 `→ rep=NSSymbolImageRep`。**遗留**：现在所有视觉走 subview，SwiftUI label 故意画成 1x1 透明方块保 MenuBarExtraAccess bridge 能正常工作。如果未来要做动态内容（loading/error/各种 SF Symbol），得让 `ModernApp.swift` 的 label 拿到的 SF Symbol 也跟着 `currentIconSymbolName` 变 ——这条路侵入性比单纯控制 button.image 大，详见 `docs/handoffs/2026-07-05-token-king-icon-blurry.md` |
| B42 | OpenCode Go / OpenCode Zen 看不到套餐选择 | ✅已修（commit `81881d8`） | `ProviderProtocol.family` 把 `.openCode` / `.openCodeZen` / `.openCodeGo` 都映射到同一个 `.openCode` 家庭。`SubscriptionSettings.presets(family:region:)` 的 `case (.openCode, _): return openCode` 返回空数组——结果 `.openCodeZen`（定义 `openCodeZen: []`）跟 `.openCodeGo`（定义 `[SubscriptionPreset(name: "Go", cost: 10)]`）都各自有预设但 family switch 永远走 `.openCode` 分支拿不到，菜单显示「按量计费，无预置订阅套餐」。通过 `ProviderSubscriptionPresets.findLikelyDuplicateSubscriptionKeys()` 实测也确认：用户机器上 `subscription_v2.opencode_go._default_ = {"name":"Go","amount":10,"type":"preset"}` 一直存着，但菜单无法选也找不到 | commit `81881d8`：`ProviderFamily` 加 `case openCodeZen` / `case openCodeGo`；`var family` 给 `.openCodeZen` / `.openCodeGo` 各自映射独立 family；`presets(family:region:)` 加 `case (.openCodeZen, _): return openCodeZen` 跟 `case (.openCodeGo, _): return openCodeGo`。351/351 测试通过 |
| B43 | Kimi / OpenCodeGo 「套餐」header 显示 API 检测结果，跟用户选的 ✓ 不一致 | ✅已修（commit `81881d8`） | `ProviderMenuBuilder.swift` 里 `.kimi` / `.openCodeGo` case 在显示套餐时直接 `createDisabledLabelView(text: "套餐：\(details.planType)")`——这是 API 返回的检测结果（如 Moderato ¥99）。但用户可以在菜单里手动选另一档（如 Allegretto ¥199）并打勾；header 跟勾同时显示两个不同套餐，用户视觉上冲突 | commit `81881d8`：header 改成优先显示 `currentPlan.displayTitle(...)`（用户在 Token King 里存的当前订阅）；fall back 到 `details.planType`（API 检测）只在 `currentPlan.isSet == false` 时。这样 header 跟勾始终对齐 |
| B44 | 「额度状态 ¥1,330/月」跟用户心理预期不一致 | ✅已修（commit `81881d8`）| 实测 5 笔 user-defaults subscription key 算出 1330：`kimi_cn.d7k…Allegretto¥199` (cnyCost 命中) + `kimi.d7k…Allegretto$39`（无 cnyCost，39×6.795=265 RMB） + `codex.smy…Pro$100`（100×6.795=680 RMB） + `minimax_cn._default_Max¥119` + `opencode_go._default_Go$10`。其中 `kimi`/`kimi_cn` 同一账号存了两份 Allegretto 是用户从 Kimi Global 切到 Kimi CN 时没清老的；`codex` 不知何时从 Plus $20 改成 Pro $100；缓存汇率 6.795 是 6/30 fetch 的 | commit `81881d8`：`SubscriptionSettingsManager.findLikelyDuplicateSubscriptionKeys()` 按 `accountId` 后缀分组，flag 同账号多个 key；`StatusBarController` 在「额度状态」header 下加警告 + 每个重复 key 一个 delete 菜单项；`@objc func removeDuplicateSubscription(_:)` handler 弹 alert 确认后删。351/351 测试通过 |
