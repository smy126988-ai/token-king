# Token King — 阶段 2（货币 RMB + 汇率）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 Token King 支持人民币显示：新增 CurrencyFormatter + ExchangeRateStore（方案C：本地默认汇率 + 拉取成功回写缓存），加菜单开关切换 USD/RMB，把散落的 `$%.2f` 货币渲染统一走 formatter。

**Architecture:** 三层。`ExchangeRateStore` 管汇率持久化（UserDefaults 缓存 + 默认值 + 异步拉取 open.er-api.com 回写，stale-while-revalidate）。`CurrencyFormatter`（共享单例）读取当前币种偏好 + store 的汇率，把 USD 金额格式化成 `$x.xx` 或 `¥x.xx`。渲染层（StatusBarController 及其 extension ProviderMenuBuilder）的货币格式化全部改走 `CurrencyFormatter.shared.format(usd:)`。币种偏好用 UserDefaults，菜单加切换项。

**Tech Stack:** Swift 6.3 / Xcode 26 / XCTest / xcodebuild。工程在 `~/projects/usage-deck/CopilotMonitor/`，scheme = `CopilotMonitor`，测试 module = `OpenCode_Bar`。

**关键事实（已摸清，实现时不要重新假设）：**
- 测试命令：`cd ~/projects/usage-deck/CopilotMonitor && xcodebuild test -scheme CopilotMonitor -destination 'platform=macOS' 2>&1 | tail -30`
- 单测某个类：加 `-only-testing:CopilotMonitorTests/<ClassName>`
- 构建命令：把 `test` 换成 `build`
- fixture 机制：`Bundle(for: type(of: self)).url(forResource:withExtension:)`，fixture 文件需加入测试 target 的 Copy Bundle Resources
- 测试导入：`@testable import OpenCode_Bar`
- **汇率 API（已实测可用，免费无 key）**：`GET https://open.er-api.com/v6/latest/USD` → JSON `{ "result": "success", "rates": { "CNY": 6.79... }, "time_last_update_utc": "..." }`。失败时 `result` 非 `"success"`。
- **货币渲染现状**：`$%.2f` / `$%.0f` 散在 `CopilotMonitor/App/StatusBarController.swift`（4355 行）和 `CopilotMonitor/Helpers/ProviderMenuBuilder.swift`（1786 行，实为 `extension StatusBarController`）。状态栏统一入口是 `formatCostForStatusBar(_:)`（StatusBarController.swift:885）。
- **preferences 模式**：`enum XxxPreferences { static let key = "..." }` + UserDefaults，见 `CopilotMonitor/Helpers/MenuEnums.swift:122` 的 `StatusBarDisplayPreferences`
- **URLSession 注入范本**：`TavilySearchProvider` 的 `init(session: URLSession = .shared)`
- **provider 显隐已存在**：`isProviderEnabled()` / `toggleProvider()`（StatusBarController.swift:673）已实现，阶段2「菜单精简-隐藏provider」需求已满足，本计划不重复做
- **百分比渲染（`%.1f%%` 等）不是货币**，保持不动

---

## File Structure

- Create: `CopilotMonitor/CopilotMonitor/Services/ExchangeRateStore.swift` — 汇率拉取 + 缓存回写 + 默认值
- Create: `CopilotMonitor/CopilotMonitor/Services/CurrencyFormatter.swift` — 币种偏好 + USD→显示字符串
- Modify: `CopilotMonitor/CopilotMonitor/Helpers/MenuEnums.swift` — 加 `CurrencyPreferences`
- Modify: `CopilotMonitor/CopilotMonitor/App/StatusBarController.swift` — 币种 getter/setter、菜单项、启动拉汇率、货币渲染改走 formatter
- Modify: `CopilotMonitor/CopilotMonitor/Helpers/ProviderMenuBuilder.swift` — 货币渲染改走 formatter
- Create: `CopilotMonitor/CopilotMonitorTests/ExchangeRateStoreTests.swift`
- Create: `CopilotMonitor/CopilotMonitorTests/CurrencyFormatterTests.swift`
- Create: `CopilotMonitor/CopilotMonitorTests/Fixtures/exchange_rate_success.json`
- Create: `CopilotMonitor/CopilotMonitorTests/Fixtures/exchange_rate_error.json`

---

## 阶段 2A：货币核心（纯逻辑，TDD）

### Task 2A.1：ExchangeRateStore — 汇率解析 + 缓存回写

**Files:**
- Create: `CopilotMonitor/CopilotMonitor/Services/ExchangeRateStore.swift`
- Create: `CopilotMonitor/CopilotMonitorTests/ExchangeRateStoreTests.swift`
- Create: `CopilotMonitor/CopilotMonitorTests/Fixtures/exchange_rate_success.json`
- Create: `CopilotMonitor/CopilotMonitorTests/Fixtures/exchange_rate_error.json`

- [ ] **Step 1：写两个 fixture**

Create `CopilotMonitorTests/Fixtures/exchange_rate_success.json`:
```json
{
  "result": "success",
  "base_code": "USD",
  "time_last_update_utc": "Wed, 01 Jul 2026 00:02:31 +0000",
  "rates": {
    "USD": 1,
    "CNY": 7.2531
  }
}
```

Create `CopilotMonitorTests/Fixtures/exchange_rate_error.json`:
```json
{
  "result": "error",
  "error-type": "unsupported-code"
}
```

- [ ] **Step 2：写失败测试**

Create `CopilotMonitorTests/ExchangeRateStoreTests.swift`:
```swift
import XCTest
@testable import OpenCode_Bar

final class ExchangeRateStoreTests: XCTestCase {
    private func fixtureData(_ name: String) throws -> Data {
        let bundle = Bundle(for: type(of: self))
        let url = bundle.url(forResource: name, withExtension: "json")!
        return try Data(contentsOf: url)
    }

    private func makeDefaults() -> UserDefaults {
        let suite = "ExchangeRateStoreTests.\(UUID().uuidString)"
        let defaults = UserDefaults(suiteName: suite)!
        defaults.removePersistentDomain(forName: suite)
        return defaults
    }

    func testParsesCNYRateFromSuccessResponse() throws {
        let rate = try ExchangeRateStore.parseUSDToCNY(from: fixtureData("exchange_rate_success"))
        XCTAssertEqual(rate, 7.2531, accuracy: 0.0001)
    }

    func testParseThrowsOnErrorResponse() throws {
        XCTAssertThrowsError(try ExchangeRateStore.parseUSDToCNY(from: fixtureData("exchange_rate_error")))
    }

    func testRateReturnsDefaultWhenNoCache() {
        let store = ExchangeRateStore(defaults: makeDefaults())
        XCTAssertEqual(store.usdToCNY, ExchangeRateStore.defaultUSDToCNY, accuracy: 0.0001)
    }

    func testRefreshWritesBackCacheAndReadsIt() async throws {
        let defaults = makeDefaults()
        let data = try fixtureData("exchange_rate_success")
        let store = ExchangeRateStore(defaults: defaults, fetcher: { data })
        try await store.refresh()
        XCTAssertEqual(store.usdToCNY, 7.2531, accuracy: 0.0001)
        // A fresh store on the same defaults sees the written-back value (survives restart).
        let reopened = ExchangeRateStore(defaults: defaults)
        XCTAssertEqual(reopened.usdToCNY, 7.2531, accuracy: 0.0001)
    }

    func testRefreshFailureKeepsLastKnownRate() async throws {
        let defaults = makeDefaults()
        let ok = try fixtureData("exchange_rate_success")
        let store = ExchangeRateStore(defaults: defaults, fetcher: { ok })
        try await store.refresh()
        // Next refresh throws inside fetcher; store must keep 7.2531, not reset to default.
        store.fetcher = { throw ProviderError.networkError("boom") }
        _ = try? await store.refresh()
        XCTAssertEqual(store.usdToCNY, 7.2531, accuracy: 0.0001)
    }
}
```

- [ ] **Step 3：跑测试确认失败**

Run:
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild test -scheme CopilotMonitor -destination 'platform=macOS' -only-testing:CopilotMonitorTests/ExchangeRateStoreTests 2>&1 | tail -20
```
Expected: 编译失败（`ExchangeRateStore` 未定义）

- [ ] **Step 4：实现 ExchangeRateStore.swift**

Create `CopilotMonitor/Services/ExchangeRateStore.swift`:
```swift
import Foundation
import os

private let rateLogger = Logger(subsystem: "com.opencodeproviders", category: "ExchangeRateStore")

/// Holds the USD→CNY exchange rate with scheme C behaviour:
/// local default → on launch fetch live rate → on success write back to cache →
/// on failure keep the last cached value (stale-while-revalidate).
final class ExchangeRateStore {
    /// Reasonable fallback used only until the first successful fetch writes a real value.
    static let defaultUSDToCNY: Double = 7.2
    static let cacheKey = "currency.usdToCNY.cached"
    static let cacheUpdatedAtKey = "currency.usdToCNY.updatedAt"

    private let defaults: UserDefaults
    /// Injectable fetcher returning raw response Data. Real impl hits open.er-api.com.
    /// Mutable so tests can swap behaviour mid-lifecycle (e.g. success then failure).
    var fetcher: () async throws -> Data

    init(defaults: UserDefaults = .standard,
         fetcher: (() async throws -> Data)? = nil) {
        self.defaults = defaults
        self.fetcher = fetcher ?? ExchangeRateStore.liveFetch
    }

    /// Current rate: cached value if present, otherwise the default.
    var usdToCNY: Double {
        let cached = defaults.double(forKey: Self.cacheKey)
        return cached > 0 ? cached : Self.defaultUSDToCNY
    }

    /// Fetch live rate; on success write back to cache. Throws on failure (caller may ignore).
    func refresh() async throws {
        let data = try await fetcher()
        let rate = try Self.parseUSDToCNY(from: data)
        defaults.set(rate, forKey: Self.cacheKey)
        defaults.set(Date().timeIntervalSince1970, forKey: Self.cacheUpdatedAtKey)
        rateLogger.info("USD→CNY refreshed: \(rate)")
    }

    static func parseUSDToCNY(from data: Data) throws -> Double {
        struct Response: Decodable {
            let result: String
            let rates: [String: Double]
        }
        let decoded = try JSONDecoder().decode(Response.self, from: data)
        guard decoded.result == "success", let cny = decoded.rates["CNY"], cny > 0 else {
            throw ProviderError.decodingError("Exchange rate response missing CNY")
        }
        return cny
    }

    private static func liveFetch() async throws -> Data {
        guard let url = URL(string: "https://open.er-api.com/v6/latest/USD") else {
            throw ProviderError.networkError("Invalid exchange rate URL")
        }
        let (data, response) = try await URLSession.shared.data(from: url)
        guard let http = response as? HTTPURLResponse, (200...299).contains(http.statusCode) else {
            throw ProviderError.networkError("Exchange rate HTTP error")
        }
        return data
    }
}
```
注意：`ProviderError` 已存在于工程（TavilySearchProvider 用了 `.networkError` / `.decodingError`）。若编译报某 case 不存在，`grep -n "enum ProviderError" -A8 CopilotMonitor/Models/*.swift` 查实际 case 名再对齐。

- [ ] **Step 5：新文件加入工程并跑测试**

新建 `.swift` 和两个 fixture `.json` 需加入 target（fixture 加测试 target 的 Copy Bundle Resources）。用 Xcode GUI 拖入或确认 project.pbxproj 已包含。然后：
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild test -scheme CopilotMonitor -destination 'platform=macOS' -only-testing:CopilotMonitorTests/ExchangeRateStoreTests 2>&1 | tail -20
```
Expected: 5 个测试全 PASS。

- [ ] **Step 6：提交**

Run:
```bash
cd ~/projects/usage-deck
git add CopilotMonitor/CopilotMonitor/Services/ExchangeRateStore.swift CopilotMonitor/CopilotMonitorTests/ExchangeRateStoreTests.swift CopilotMonitor/CopilotMonitorTests/Fixtures/exchange_rate_success.json CopilotMonitor/CopilotMonitorTests/Fixtures/exchange_rate_error.json CopilotMonitor/CopilotMonitor.xcodeproj/project.pbxproj
git commit -m "feat: add ExchangeRateStore with scheme-C cache writeback"
```

### Task 2A.2：CurrencyFormatter — 币种偏好 + 格式化

**Files:**
- Create: `CopilotMonitor/CopilotMonitor/Services/CurrencyFormatter.swift`
- Modify: `CopilotMonitor/CopilotMonitor/Helpers/MenuEnums.swift`（加 `CurrencyPreferences`）
- Create: `CopilotMonitor/CopilotMonitorTests/CurrencyFormatterTests.swift`

- [ ] **Step 1：在 MenuEnums.swift 末尾加 CurrencyPreferences**

在 `CopilotMonitor/Helpers/MenuEnums.swift` 文件末尾追加：
```swift
enum Currency: String, CaseIterable {
    case usd = "USD"
    case rmb = "RMB"

    var symbol: String {
        switch self {
        case .usd: return "$"
        case .rmb: return "¥"
        }
    }

    var menuTitle: String {
        switch self {
        case .usd: return "US Dollar ($)"
        case .rmb: return "人民币 (¥)"
        }
    }
}

enum CurrencyPreferences {
    static let selectedCurrencyKey = "currency.selected"
}
```

- [ ] **Step 2：写失败测试**

Create `CopilotMonitorTests/CurrencyFormatterTests.swift`:
```swift
import XCTest
@testable import OpenCode_Bar

final class CurrencyFormatterTests: XCTestCase {
    private func makeDefaults() -> UserDefaults {
        let suite = "CurrencyFormatterTests.\(UUID().uuidString)"
        let defaults = UserDefaults(suiteName: suite)!
        defaults.removePersistentDomain(forName: suite)
        return defaults
    }

    func testDefaultsToUSD() {
        let f = CurrencyFormatter(defaults: makeDefaults(), rateStore: ExchangeRateStore(defaults: makeDefaults()))
        XCTAssertEqual(f.currency, .usd)
    }

    func testFormatsUSDUnchanged() {
        let f = CurrencyFormatter(defaults: makeDefaults(), rateStore: ExchangeRateStore(defaults: makeDefaults()))
        f.currency = .usd
        XCTAssertEqual(f.format(usd: 12.5), "$12.50")
    }

    func testFormatsRMBUsingRate() {
        let defaults = makeDefaults()
        let rateDefaults = makeDefaults()
        rateDefaults.set(7.0, forKey: ExchangeRateStore.cacheKey)
        let f = CurrencyFormatter(defaults: defaults, rateStore: ExchangeRateStore(defaults: rateDefaults))
        f.currency = .rmb
        XCTAssertEqual(f.format(usd: 10.0), "¥70.00")
    }

    func testCurrencyPreferencePersists() {
        let defaults = makeDefaults()
        let f = CurrencyFormatter(defaults: defaults, rateStore: ExchangeRateStore(defaults: makeDefaults()))
        f.currency = .rmb
        let reopened = CurrencyFormatter(defaults: defaults, rateStore: ExchangeRateStore(defaults: makeDefaults()))
        XCTAssertEqual(reopened.currency, .rmb)
    }

    func testFormatWithDecimalsZero() {
        let f = CurrencyFormatter(defaults: makeDefaults(), rateStore: ExchangeRateStore(defaults: makeDefaults()))
        f.currency = .usd
        XCTAssertEqual(f.format(usd: 42.0, decimals: 0), "$42")
    }
}
```

- [ ] **Step 3：跑测试确认失败**

Run:
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild test -scheme CopilotMonitor -destination 'platform=macOS' -only-testing:CopilotMonitorTests/CurrencyFormatterTests 2>&1 | tail -20
```
Expected: 编译失败（`CurrencyFormatter` 未定义）

- [ ] **Step 4：实现 CurrencyFormatter.swift**

Create `CopilotMonitor/Services/CurrencyFormatter.swift`:
```swift
import Foundation

/// Formats USD amounts into the user's selected display currency.
/// USD amounts are the source of truth everywhere; this converts + renders at the edge.
final class CurrencyFormatter {
    static let shared = CurrencyFormatter()

    private let defaults: UserDefaults
    private let rateStore: ExchangeRateStore

    init(defaults: UserDefaults = .standard,
         rateStore: ExchangeRateStore = ExchangeRateStore()) {
        self.defaults = defaults
        self.rateStore = rateStore
    }

    var currency: Currency {
        get {
            guard let raw = defaults.string(forKey: CurrencyPreferences.selectedCurrencyKey),
                  let c = Currency(rawValue: raw) else { return .usd }
            return c
        }
        set { defaults.set(newValue.rawValue, forKey: CurrencyPreferences.selectedCurrencyKey) }
    }

    /// Convert a USD amount into the active currency and render with symbol.
    func format(usd amount: Double, decimals: Int = 2) -> String {
        let converted: Double
        switch currency {
        case .usd: converted = amount
        case .rmb: converted = amount * rateStore.usdToCNY
        }
        return "\(currency.symbol)\(String(format: "%.\(decimals)f", converted))"
    }

    /// Kick a background rate refresh (call on launch). Failures are silently ignored.
    func refreshRateInBackground() {
        Task { try? await rateStore.refresh() }
    }
}
```

- [ ] **Step 5：加入工程并跑测试**

Run:
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild test -scheme CopilotMonitor -destination 'platform=macOS' -only-testing:CopilotMonitorTests/CurrencyFormatterTests 2>&1 | tail -20
```
Expected: 5 个测试全 PASS。

- [ ] **Step 6：提交**

Run:
```bash
cd ~/projects/usage-deck
git add CopilotMonitor/CopilotMonitor/Services/CurrencyFormatter.swift CopilotMonitor/CopilotMonitor/Helpers/MenuEnums.swift CopilotMonitor/CopilotMonitorTests/CurrencyFormatterTests.swift CopilotMonitor/CopilotMonitor.xcodeproj/project.pbxproj
git commit -m "feat: add CurrencyFormatter with USD/RMB support"
```

---

## 阶段 2B：菜单开关 + 启动拉汇率

### Task 2B.1：加币种切换菜单项 + 启动刷新汇率

**Files:**
- Modify: `CopilotMonitor/CopilotMonitor/App/StatusBarController.swift`

- [ ] **Step 1：定位菜单构建处，确认插入锚点**

Run（找 statusBarOptionsMenu 里加 showProviderName 那处，币种项加在附近）：
```bash
cd ~/projects/usage-deck/CopilotMonitor
grep -n "statusBarOptionsMenu.addItem\|showProviderNameMenuItem = NSMenuItem" CopilotMonitor/App/StatusBarController.swift | head
```
Expected: 看到 `showProviderNameMenuItem = NSMenuItem(...)` 和 `statusBarOptionsMenu.addItem(showProviderNameMenuItem)`（约 479-481 行）。币种子菜单加在该 addItem 之后。

- [ ] **Step 2：在 StatusBarController 加币种子菜单构建 + 切换 action**

在 StatusBarController 类内（`isProviderEnabled` 附近，约 679 行后）加：
```swift
    private func buildCurrencyMenu() -> NSMenuItem {
        let parent = NSMenuItem(title: "Currency", action: nil, keyEquivalent: "")
        let submenu = NSMenu()
        for currency in Currency.allCases {
            let item = NSMenuItem(title: currency.menuTitle,
                                  action: #selector(selectCurrency(_:)),
                                  keyEquivalent: "")
            item.target = self
            item.representedObject = currency.rawValue
            item.state = (CurrencyFormatter.shared.currency == currency) ? .on : .off
            submenu.addItem(item)
        }
        parent.submenu = submenu
        return parent
    }

    @objc private func selectCurrency(_ sender: NSMenuItem) {
        guard let raw = sender.representedObject as? String,
              let currency = Currency(rawValue: raw) else { return }
        CurrencyFormatter.shared.currency = currency
        updateStatusBarText()
        refreshClicked()
    }
```

- [ ] **Step 3：把币种菜单挂到 options 菜单**

在 `statusBarOptionsMenu.addItem(showProviderNameMenuItem)`（约 481 行）之后加一行：
```swift
        statusBarOptionsMenu.addItem(buildCurrencyMenu())
```

- [ ] **Step 4：启动时拉一次汇率**

在 `override init()`（StatusBarController.swift:276）的 `super.init()` 之后加：
```swift
        CurrencyFormatter.shared.refreshRateInBackground()
```

- [ ] **Step 5：构建验证**

Run:
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild build -scheme CopilotMonitor -destination 'platform=macOS' 2>&1 | tail -5
```
Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 6：提交**

Run:
```bash
cd ~/projects/usage-deck
git add CopilotMonitor/CopilotMonitor/App/StatusBarController.swift
git commit -m "feat: add currency switcher menu and launch rate refresh"
```

---

## 阶段 2C：渲染层改走 formatter

> 货币格式化散在两文件几十处。策略：状态栏统一入口 `formatCostForStatusBar` 先改（覆盖状态栏所有 cost 显示）；再扫 ProviderMenuBuilder 的 cost 行。百分比（`%%`）不动。

### Task 2C.1：状态栏 cost 入口改走 formatter

**Files:**
- Modify: `CopilotMonitor/CopilotMonitor/App/StatusBarController.swift:885`

- [ ] **Step 1：改 formatCostForStatusBar**

把 `formatCostForStatusBar`（StatusBarController.swift:885）的实现：
```swift
    private func formatCostForStatusBar(_ cost: Double) -> String {
        String(format: "$%.2f", cost)
    }
```
改为：
```swift
    private func formatCostForStatusBar(_ cost: Double) -> String {
        CurrencyFormatter.shared.format(usd: cost)
    }
```

- [ ] **Step 2：扫状态栏内联 `$%.2f` cost 点并逐个替换**

Run 列出 StatusBarController 里的货币内联点：
```bash
cd ~/projects/usage-deck/CopilotMonitor
grep -n 'String(format: "\$%\.\|String(format: "%@ (\$%\.\|Pay-as-you-go: \$\|Quota Status: \$\|Orphaned (\$' CopilotMonitor/App/StatusBarController.swift
```
对每个**面向用户显示**的货币点（排除 `debugLog`/`logger` 里的），用 Edit 替换。示例转换规则：
- `String(format: "$%.2f", x)` → `CurrencyFormatter.shared.format(usd: x)`
- `String(format: "%@ ($%.2f)", name, x)` → `"\(name) (\(CurrencyFormatter.shared.format(usd: x)))"`
- `String(format: "Pay-as-you-go: $%.2f", x)` → `"Pay-as-you-go: \(CurrencyFormatter.shared.format(usd: x))"`
- `String(format: "Quota Status: $%.0f/m", x)` → `"Quota Status: \(CurrencyFormatter.shared.format(usd: x, decimals: 0))/m"`
- `String(format: "Orphaned ($%.2f)", x)` → `"Orphaned (\(CurrencyFormatter.shared.format(usd: x)))"`

**只改用户可见的显示串，debugLog/logger 里的 `$%.2f` 保持不动**（那是内部日志，不面向用户）。

- [ ] **Step 3：构建 + 全量测试**

Run:
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild test -scheme CopilotMonitor -destination 'platform=macOS' 2>&1 | tail -15
```
Expected: BUILD 成功，全部测试 PASS（无回归）。

- [ ] **Step 4：提交**

Run:
```bash
cd ~/projects/usage-deck
git add CopilotMonitor/CopilotMonitor/App/StatusBarController.swift
git commit -m "refactor: route status bar cost rendering through CurrencyFormatter"
```

### Task 2C.2：ProviderMenuBuilder cost 行改走 formatter

**Files:**
- Modify: `CopilotMonitor/CopilotMonitor/Helpers/ProviderMenuBuilder.swift`

- [ ] **Step 1：扫 ProviderMenuBuilder 货币点**

Run:
```bash
cd ~/projects/usage-deck/CopilotMonitor
grep -n 'String(format: "[^"]*\$%\.' CopilotMonitor/Helpers/ProviderMenuBuilder.swift
```
Expected: 看到 `Credits: $%.0f`、`Avg/Day: $%.2f`、`  %@: $%.2f`、`Limit: $%.2f/m`、`Used: $%.2f`、`Credits: $%.2f` 等行。

- [ ] **Step 2：逐个替换为 formatter 调用**

对每行按 2C.1 Step 2 的规则转换。示例：
- `String(format: "Credits: $%.0f", remaining)` → `"Credits: \(CurrencyFormatter.shared.format(usd: remaining, decimals: 0))"`
- `String(format: "Avg/Day: $%.2f", avg)` → `"Avg/Day: \(CurrencyFormatter.shared.format(usd: avg))"`
- `String(format: "  %@: $%.2f", shortName, cost)` → `"  \(shortName): \(CurrencyFormatter.shared.format(usd: cost))"`
- `String(format: "Limit: $%.2f/m", limitUSD)` → `"Limit: \(CurrencyFormatter.shared.format(usd: limitUSD))/m"`
- `String(format: "Used: $%.2f", usedUSD)` → `"Used: \(CurrencyFormatter.shared.format(usd: usedUSD))"`

- [ ] **Step 3：构建 + 全量测试**

Run:
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild test -scheme CopilotMonitor -destination 'platform=macOS' 2>&1 | tail -15
```
Expected: BUILD 成功，全部测试 PASS。

- [ ] **Step 4：真机手动验证（关键 — 单测覆盖不了真实显示）**

Run 构建启动，切到人民币看菜单：
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild build -scheme CopilotMonitor -configuration Debug -derivedDataPath /tmp/tk-build -destination 'platform=macOS' 2>&1 | tail -5
pkill -f "OpenCode Bar" 2>/dev/null; pkill -f "Token King" 2>/dev/null
open /tmp/tk-build/Build/Products/Debug/*.app
```
Expected: 菜单 Options → Currency → 选「人民币 (¥)」后，所有 cost 显示从 `$x.xx` 变 `¥y.yy`（y≈x×7.2 或实时汇率）。切回 US Dollar 恢复 `$`。**这一步必须人工看菜单确认。**

- [ ] **Step 5：提交**

Run:
```bash
cd ~/projects/usage-deck
git add CopilotMonitor/CopilotMonitor/Helpers/ProviderMenuBuilder.swift
git commit -m "refactor: route provider menu cost rendering through CurrencyFormatter"
```

---

## 阶段 2 验收

- [ ] **Step 1：全量测试通过**

Run:
```bash
cd ~/projects/usage-deck/CopilotMonitor
xcodebuild test -scheme CopilotMonitor -destination 'platform=macOS' 2>&1 | tail -10
```
Expected: 全部 PASS（含 2A 新增的 10 个测试，且无回归）。

- [ ] **Step 2：逐项对照验收**
  - [ ] ExchangeRateStore：解析 CNY、默认值、缓存回写、失败保留（ExchangeRateStoreTests PASS）
  - [ ] CurrencyFormatter：USD/RMB 格式化、偏好持久化（CurrencyFormatterTests PASS）
  - [ ] 菜单有 Currency 切换项，能切 USD/RMB（Task 2C.2 Step 4 人工确认）
  - [ ] 切人民币后 cost 显示变 ¥ 且按汇率换算（人工确认）
  - [ ] 启动会拉实时汇率并回写（可选：删 UserDefaults 缓存后启动，看日志 `USD→CNY refreshed`）
  - [ ] 菜单精简-隐藏provider：**上游已实现，本阶段标记已满足**（isProviderEnabled）
  - [ ] 无回归（其他 provider 测试仍 PASS）

- [ ] **Step 3：收口**

进入 `superpowers:finishing-a-development-branch`，验证测试 → 给出 merge/PR/保留/丢弃选项 → 执行。

---

## 残留 / 后续

- **CommandCodeProvider 的 usageSummary 未走 formatter（final review 发现，2026-07-01 记）**：`CommandCodeProvider.swift:120` 的 `usageSummary` 在数据层就把 `$` 烤进字符串（`"%@ · $%.2f of $%.2f"`），经 `authUsageSummary` 渲染在 provider 详情菜单 "Using in:" 行。切人民币时这一行仍显示 `$`，其余所有 cost 已正确转 ¥。**未在本阶段修复的原因**：① CommandCodeProvider 被 `opencodebar-cli` target 也编译，而 CurrencyFormatter 仅在主 app target，直接调用导致 CLI 编译失败；② 根因是数据层预烤 cost，违反"USD 数据层唯一真值、渲染边缘转换"原则，正确修法要重构多 provider 共用的 authUsageSummary 渲染逻辑，超出本阶段 scope。**建议修法（架构正确）**：`usageSummary` 不再预烤 `$`，改在 ProviderMenuBuilder 用 `DetailedUsage` 已有的结构化 `monthlyCost`/`creditsTotal` 字段重组显示串。留待阶段3/后续统一处理。
- **菜单字段级精简**（隐藏 provider 内的某些行，如 reset 时间/plan 名）：本计划未做，YAGNI。若将来需要，另写计划，需动 StatusBarController 4355 行的渲染逻辑。
- **阶段 3（中文化）、阶段 4（widget）**：待阶段2完成后再写详细计划。
- **汇率端点冗余**：当前只用 open.er-api.com。若该服务不稳，可在 ExchangeRateStore.liveFetch 加备用端点，届时另议。
